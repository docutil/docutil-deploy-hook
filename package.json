{
  "name": "docutil-deploy-hook",
  "version": "2.0.0",
  "type": "module",
  "author": "zhanlefeng",
  "main": "dist/index.mjs",
  "devDependencies": {
    "@types/bun": "^1.0.12"
  },
  "license": "MIT",
  "scripts": {
    "start": "bun run --smol dist/index.js",
    "build": "bun build --outfile=dist/index.js --format=esm --target=bun --bundle --minify src/index.js"
  },
  "dependencies": {
    "zx": "^8.0.1"
  },
  "engines": {
    "bun": "^1.1.0"
  }
}
