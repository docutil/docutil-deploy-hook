{
  "name": "docutil-deploy-hook",
  "version": "2.0.0",
  "author": "zhanlefeng",
  "main": "dist/index.mjs",
  "devDependencies": {
    "@types/bun": "^1.0.12"
  },
  "license": "MIT",
  "scripts": {
    "start": "bun dist/index.mjs",
    "build": "bun build --outfile=dist/index.mjs --format=esm --target=bun --bundle --minify src/index.js"
  },
  "dependencies": {
    "zx": "^8.0.1"
  }
}
