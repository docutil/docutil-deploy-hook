{
  "name": "docutil-deploy-hook",
  "version": "2.0.0",
  "author": "zhanlefeng",
  "main": "dist/index.mjs",
  "devDependencies": {
    "@types/bun": "^1.0.12",
    "esbuild": "^0.20.2"
  },
  "license": "MIT",
  "scripts": {
    "start": "node dist/index.mjs",
    "build": "esbuild --outfile=dist/index.mjs --external:bun --format=esm --platform=node --target=node20 --bundle --minify src/index.js"
  },
  "dependencies": {
    "zx": "^8.0.1"
  }
}
